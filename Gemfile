source "http://rubygems.org"

# Specify your gem's dependencies in capistrano-exts.gemspec
gemspec

# gemspec don't support test group, we need to defined them here

group :test do
  gem 'rspec', ">=2.6.0"
end